import React from 'react';
import { Typography } from '@mui/material';

const Message = () => {
  
  

  return (
    <div>
      <Typography variant="h4" gutterBottom> Message </Typography>
      <Typography variant="body1">ข้อความของคุณ </Typography>
    </div>
  )
  
};

export default Message;
